# NewsBot 실험 리포트

> 실측 기반 실험 기록. 모든 수치는 `data/newsbot.db` 실제 조회
> (`python db_check.py`)와 `fetch` 로그에서 추출했다.

---

## 1. RSS vs 크롤링 — 실측 품질 비교

### 1-1. 본문 길이 (clean_news 기준, DB 실측 ①번 조회)

| 방법  |  건수 | 평균 본문   | 최소 |   최대 |
|-------|------:|------------:|-----:|-------:|
| crawl |     8 | **1,655자** |  662 |  2,876 |
| rss   |     4 |   **710자** |   83 |  1,802 |

→ 크롤링이 RSS 대비 본문 **약 2.3배**. 상세 페이지 파싱의 효과 확인.

### 1-2. 메타데이터 결측 (DB 실측 ②번 조회)

| 방법  | 기자명 없음 | 카테고리 없음 |
|-------|:-----------:|:-------------:|
| crawl |      0      |       0       |
| rss   |      0      |       0       |

→ 두 방법 모두 기자명·카테고리 결측 없음.
`missing_author: "미상"`, `missing_category: "미분류"` 기본값 처리가
정상 동작한 결과다. (config.json `cleaning` 섹션)

### 1-3. 정제 단계 결과 (fetch 로그 기준)

| 실험              | 신규 | 중복 | 폐기 | 사유                      |
|-------------------|-----:|-----:|-----:|---------------------------|
| RSS(--no-content) |    1 |    3 |    1 | 본문 너무 짧음(raw_id=36) |
| RSS(본문 보완)    |    0 |    5 |    0 | 앞 실험과 중복            |
| crawl             |    0 |    5 |    0 | RSS와 URL 중복            |

### 1-4. 분석

- **RSS 최소 본문이 83자**로 폐기 기준(min_content_length=60,
  config.json 확정값)에 근접. 실제로 짧은 본문 1건이 폐기됨
  → RSS description 방식의 품질 한계 실측.
- RSS 본문이 83자로 짧은 이유: `content_fallback_to_description: true` 설정으로
  본문 파싱 실패 시 RSS description으로 폴백하기 때문.
  description이 짧으면 본문도 짧아진다. (config.json `cleaning` 섹션)
- 크롤링은 최소 662자로 폐기 위험 없음. 대신 RSS와 URL이 겹쳐
  **신규 0건(전부 중복)** → 방법 병용 시 dedup이 필수임을 확인.

### 1-5. 설계 결정

| 근거                    | 결정                       | 코드 위치                    |
|-------------------------|----------------------------|------------------------------|
| RSS 본문 83자로 부실    | 크롤링은 상세 페이지 파싱  | `crawler.py _fetch_article`  |
| 방법 간 URL 중복        | url_hash dedup             | `crawler.py`, `storage.py`   |
| 부실 본문 폐기 필요     | min_content_length=60 기준 | `cleaner.py`                 |
| 메타데이터 결측 없음    | 기본값 처리 정상 동작      | `cleaner.py`, `config.json`  |

---

## 2. 프롬프트 개선 과정

`summarizer.py`의 프롬프트와 후처리 코드에는 **3단계 개선의 흔적**이
그대로 남아 있다. 아래는 코드 근거로 재구성한 개선 이력이다.

### 2-1. 1차 (기본형) → 문제: 출력 형식 불안정

```
"다음 뉴스를 요약해 주세요.\n\n{content}"
```

**문제:** 모델이 `"요약: ..."`, `"이 기사는 ..."` 같은 머리말을 붙이고,
길이도 들쭉날쭉했다.

### 2-2. 2차 (형식 강제) → 개선: 출력 규칙 명시

현재 `USER_PROMPT`에 다음 제약이 추가됨:

```
- {max_chars}자 이내
- 2~3문장
- 핵심 사실(누가/무엇을/어떻게)을 우선 포함
- 머리말("이 기사는", "요약:") 없이 요약문만 출력
```

> **근거:** 프롬프트에 `머리말 ... 없이` 규칙이 명시되어 있다는 것은,
> 실제로 머리말이 붙는 문제를 겪고 대응했다는 화석 기록이다.

### 2-3. 3차 (환각 방지 + 후처리 방어) → 개선: 안정화

① `SYSTEM_PROMPT`에 환각 방지 제약 추가:

```
"기사에 실제로 있는 사실만 사용하고, 추측이나 배경 설명을 덧붙이지 마세요."
```

② 프롬프트만으로 부족한 경우를 대비해 **코드 레벨 후처리** 추가:

```python
def _clean_summary(text):
    cleaned = text.strip().strip('"').strip("'").strip()
    for prefix in ("요약:", "요약문:", "다음은", "이 기사는"):
        if cleaned.startswith(prefix):
            cleaned = cleaned[len(prefix):].lstrip(" :-")
    return cleaned.strip()
```

> **핵심 교훈:** 프롬프트로 "형식을 요청"하되, 그래도 어길 수 있으므로
> **코드로 한 번 더 방어**한다. (프롬프트 + 후처리 이중 안전장치)

### 2-4. 파라미터 튜닝

| 항목        | 값                      | 의도                          |
|-------------|-------------------------|-------------------------------|
| temperature | 0.2                     | 요약은 창의성보다 일관성 우선 |
| max_tokens  | `max(256, max_chars*3)` | 한글 토큰 여유 확보           |
| max_input   | 4,000자                 | 긴 본문 truncate로 비용 방어  |

---

## 3. 토큰 / 비용 최적화

### 3-1. 본문 길이별 압축률 (DB 실측 ③번 조회)

| id |  본문길이 | 요약길이 |     압축률 |
|---:|----------:|---------:|-----------:|
|  6 |     2,876 |      200 |       7.0% |
| 10 |     2,461 |      188 |       7.6% |
|  5 |     2,055 |      195 |       9.5% |
|  1 |     1,802 |      194 |      10.8% |
|  9 |     1,693 |      200 |      11.8% |
| 11 |     1,258 |      194 |      15.4% |
|  3 |       973 |      199 |      20.5% |
|  4 |       762 |      191 |      25.1% |
|  7 |       662 |      199 |      30.1% |
|  2 |       194 |      194 | **100.0%** |

> 전체 10건. clean_news 중 요약이 완료된 레코드 전수.

### 3-2. 분석

- **요약 길이는 188~200자로 거의 일정**. 즉 요약기는 사실상
  "고정 길이(~200자)" 출력을 낸다.
- 따라서 **본문이 길수록 압축률이 좋아진다** (2,876자 → 7.0%).
- 반대로 **짧은 본문은 압축 효과가 없다** (194자 → 100.0%).
  id=2는 본문 194자인데 요약도 194자 — 원문을 거의 그대로 반환한 것으로,
  정제는 통과(min_content_length=60 기준)했으나 **요약 품질은 사실상 실패**.
- 이는 `content_fallback_to_description: true`와 `min_content_length: 60`
  두 설정의 상호작용이 만든 사각지대다.
  **"정제 통과 ≠ 요약 품질 보장"** 임을 데이터로 확인.

### 3-3. 최적화 결론

| 관찰                       | 최적화 방안                                           |
|----------------------------|-------------------------------------------------------|
| max_input 4,000자 truncate | 초과분 토큰 비용 사전 차단                            |
| 요약 길이 고정(~200자)     | 짧은 본문은 요약 자체가 비효율                        |
| 194자 본문 → 압축률 100%   | **300자 미만은 요약 스킵** 검토 → API 호출·비용 절감 |

---

## 4. 데이터 불일치 디버깅 — raw 12 → clean 7

### 4-1. 문제 인식

report 출력상 raw 12건이 clean 7건으로 줄어 통과율 58.3%.
"5건이 어디서 사라졌는가?"를 단계별로 추적했다.

### 4-2. 원인 분해 (로그 + DB 대조)

| 소실 원인           | 건수 | 근거                      |
|---------------------|-----:|---------------------------|
| 중복(url_hash 동일) |    5 | 정제 로그 "중복 5건 skip" |
| 폐기(본문 짧음)     |    0 | 이번 실행에서는 폐기 없음 |
| 정상 통과           |    7 | clean_news 최종           |

**중복 5건의 발생 원인:** `--method all` 사용 시 RSS와 크롤링이 같은 기사를
각각 수집한다. RSS URL(`feeds.yna.co.kr/...`)과 크롤링 URL(`www.yna.co.kr/...`)은
형태가 달라 raw 단계에서는 중복 감지가 안 되고, clean 단계의 url_hash 비교에서
최종 정리된다. → 이것이 raw/clean 분리 저장의 실질적 가치다.

### 4-3. 재발 방지 (설계 반영)

> 3장 압축률 분석에서 본문이 짧을수록 요약 효율이 급격히 떨어짐을 확인.
> 정제 통과 ≠ 요약 품질 보장임을 데이터로 확인했다. (id=2, 압축률 100%)

- 요약 대상 최소 본문 길이 기준 추가 검토
  (예: `content_length < 300`이면 요약 스킵 또는 원문 표기)
- report에 "압축률 100% = 요약 미수행" 경고 지표 추가 검토

---

## 부록. 재현 방법

```bash
# 실측 데이터 재현
python db_check.py    # ①본문 길이  ②메타데이터 결측  ③압축률 조회
python main.py report # 집계 · TOP5 · 평균 압축률 확인
```

> 사용 도구: `db_check.py` (임시 조회 스크립트, `sqlite3` 모듈 사용)
> — sqlite3 CLI 미설치 환경 대응.